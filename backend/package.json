{
  "name": "backend",
  "version": "1.0.0",
  "description": "QuickSat Backend - Firebase Functions for Satellite Data APIs",
  "main": "functions/lib/index.js",
  "scripts": {
    "build": "cd functions && npm run build",
    "serve": "cd functions && npm run serve",
    "deploy": "cd functions && npm run deploy",
    "logs": "cd functions && npm run logs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["firebase", "functions", "satellite", "nasa", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "typescript": "^5.9.3"
  }
}